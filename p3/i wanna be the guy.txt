#include<bits/stdc++.h>
#define FIO ios_base::sync_with_stdio(0);cin.tie(0);
typedef long long ll;
const int MOD = 1e9 + 7,OO=0x3f3f3f3f;
const ll LOO = 0x3f3f3f3f3f3f3f3f;
const long double EPS = 1e-8;
using namespace std;
int main()
{
    FIO
    int n,p,q;
    cin>>n>>p;
    int val;
    set<char>sl;
    for(int i=0; i<p; i++)
    {
        cin>>val;
        sl.insert(val);

    }
    cin>>q;
    for(int i=0; i<q; i++)
    {
        cin>>val;
        sl.insert(val);

    }

    if(n==sl.size())cout<<"I become the guy.\n";
    else cout<<"Oh, my keyboard!\n";

    return 0;
}
