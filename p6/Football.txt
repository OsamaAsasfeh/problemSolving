#include<bits/stdc++.h>
#define FIO ios_base::sync_with_stdio(0);cin.tie(0);
typedef long long ll;
const int MOD = 1e9 + 7,OO=0x3f3f3f3f;
const ll LOO = 0x3f3f3f3f3f3f3f3f;
const long double EPS = 1e-8;
using namespace std;
int main()
{
    FIO
    long long n;
    cin>>n;
    unordered_map<string,int>mp(n);
    for(int i=0; i<n; i++)
    {
        string str;
        cin>>str;
        mp[str]++;

    }
    int mx=INT_MIN;
    string sol;
    for(auto v:mp)if(v.second>mx)
        {
            mx=v.second;
            sol=v.first;
        }
    cout<<sol;

    return 0;
}
